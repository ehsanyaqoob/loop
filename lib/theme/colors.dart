import 'package:flutter/material.dart';

class AppColors {
  // ========== PRIMARY COLORS ==========
  static const primary = Color(0xFF0066FF);
  static const primaryDark = Color(0xFF0052CC);
  static const primaryLight = Color(0xFF4D94FF);
  
  // ========== ACCENT/SECONDARY COLORS ==========
  static const accent = Color(0xFF00C853);
  static const accentDark = Color(0xFF009E40);
  static const accentLight = Color(0xFF5CFF87);
  
  // ========== NEUTRAL COLORS - LIGHT THEME ==========
  static const background = Color(0xFFF6F8FA);
  static const surface = Colors.white;
  static const onSurface = Color(0xFF1A1A1A);
  static const onBackground = Color(0xFF2D2D2D);
  
  // ========== NEUTRAL COLORS - DARK THEME ==========
  static const darkBackground = Color(0xFF0B0B0D);
  static const darkSurface = Color(0xFF121214);
  static const darkOnSurface = Colors.white;
  static const darkOnBackground = Color(0xFFE6E6E6);
  
  // ========== SEMANTIC COLORS ==========
  static const error = Color(0xFFD32F2F);
  static const success = Color(0xFF388E3C);
  static const warning = Color(0xFFFFA000);
  static const info = Color(0xFF1976D2);
  
  // ========== TEXT COLORS - LIGHT THEME ==========
  static const textPrimary = Color(0xFF1A1A1A);
  static const textSecondary = Color(0xFF666666);
  static const textDisabled = Color(0xFF999999);
  
  // ========== TEXT COLORS - DARK THEME ==========
  static const darkTextPrimary = Colors.white;
  static const darkTextSecondary = Color(0xFFB3B3B3);
  static const darkTextDisabled = Color(0xFF666666);
  
  // ========== BORDER COLORS ==========
  static const borderLight = Color(0xFFE0E0E0);
  static const borderDark = Color(0xFF333333);
  
  // ========== OVERLAY & SHADOW COLORS ==========
  static const overlayLight = Color(0x0D000000); // 5% opacity
  static const overlayDark = Color(0x1AFFFFFF);  // 10% opacity
  static const shadowLight = Color(0x1A000000);  // 10% opacity
  static const shadowDark = Color(0x33000000);   // 20% opacity
  
  // ========== STATE COLORS ==========
  static const focused = Color(0xFF0066FF);
  static const pressed = Color(0xFF0052CC);
  static const hovered = Color(0xFF4D94FF);
  static const disabled = Color(0xFFF5F5F5);
  
  // ========== GRADIENT COLORS ==========
  static const gradientStart = Color(0xFF0066FF);
  static const gradientEnd = Color(0xFF00C853);
  
  // ========== SOCIAL BRAND COLORS ==========
  static const googleRed = Color(0xFFDB4437);
  static const facebookBlue = Color(0xFF4267B2);
  static const appleBlack = Color(0xFF000000);
  static const twitterBlue = Color(0xFF1DA1F2);
}